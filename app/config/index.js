import configValues from './configValues';
import endpoints from './endpoints';
import paths from './paths';
import languages from './languages';

export { configValues, endpoints, paths, languages };
